db.security.users.by.username.query=SELECT login, password, 'true' FROM Users WHERE login=?
db.security.authorities.by.username.query=select u.login, r.role from Roles r inner JOIN UserRole ur on ur.roleId=r.id and valid=true inner join Users u on u.id=ur.userId WHERE u.login = ?